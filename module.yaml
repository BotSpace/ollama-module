apiVersion: botmother.module/v1

module:
  id: ollama
  name: Ollama
  description: Ollama orqali lokal/self-hosted LLM modellariga murojaat.
  icon: brain-circuit

source:
  github: https://github.com/BotSpace/ollama-module

runtime:
  dockerfile: Dockerfile
  port: 8100

# node'lar va credential type'lar describe() RPC'dan olinadi.
